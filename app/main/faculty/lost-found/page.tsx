import LostFoundDashboard from "@/components/lost-found/LostFoundDashboard";

export const metadata = {
  title: "Lost & Found | Faculty Dashboard",
  description: "Report and manage lost items across the campus.",
};

export default function FacultyLostFoundPage() {
  return <LostFoundDashboard />;
}